# 00 BASELINE
Objetivo: registrar el estado profesional inicial. Git no está inicializado en el workspace. Pruebas iniciales y finales se ejecutan con `npm test` y `npm run test:audit`. Supabase local y Docker fueron verificados. Pendiente: screenshots y E2E por falta de navegador conectado.
