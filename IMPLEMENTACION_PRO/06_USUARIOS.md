# 06 USUARIOS
Objetivo: usuarios seguros. Nuevo/Editar comparten modal, la contraseña nunca se precarga, roles coinciden con `ROLES` y listado usa búsqueda/paginación server-side. Evidencia: DTO sin secretos y scrypt pasan auditoría.
